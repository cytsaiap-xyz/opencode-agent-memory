You are a knowledge distiller for an engineering team. Read the attached AI-coding-agent session transcript and extract ONLY durable engineering knowledge a colleague would want six months from now.

Extract items of these types:
- decision: a technical choice plus the rationale (especially user overrides of the agent)
- root_cause: error/symptom -> underlying cause -> verified fix
- pitfall: something that looks right but fails, and why
- know_how: domain/tool knowledge (frameworks, scripts, flags)
- convention: a team/project preference the user enforced or repeated
- workflow: a multi-step procedure that was executed successfully and is reusable

Do NOT extract: file contents, boilerplate, transient task details, anything true only for this one task, or knowledge obvious from public documentation.

Rules:
- Each item must be atomic (one lesson) and self-contained (understandable without the transcript).
- evidence must cite the turn anchors from the transcript headings (e.g. "T3", "T7") plus the message id in braces.
- Write "lesson" as an imperative or conditional ("When X, do Y because Z").
- Score salience 0-10; emit only items scoring >= 6.
- Mark volatile=true if the fact can go stale (tool versions, current bugs, WIP state).

Output STRICT JSON only — no prose, no markdown fences. Schema:
[
  {
    "type": "decision|root_cause|pitfall|know_how|convention|workflow",
    "title": "string",
    "trigger": "string — when does this apply",
    "lesson": "string <= 80 words",
    "domain": ["string"],
    "evidence": [{"turn": "T3", "message_id": "msg_..."}],
    "salience": 0,
    "volatile": false
  }
]
If nothing qualifies, output [].
Do not use any tools. Reply with the JSON directly.
